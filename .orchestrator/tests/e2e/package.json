{
  "name": "orchestrator-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for the orchestrator web UI",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:plans": "playwright test plans.spec.ts",
    "test:plan-details": "playwright test plan-details.spec.ts",
    "test:build": "playwright test build.spec.ts",
    "test:visual": "percy exec -- playwright test visual/"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.8.0",
    "@percy/cli": "^1.28.0",
    "@percy/playwright": "^1.0.6",
    "@playwright/test": "^1.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
