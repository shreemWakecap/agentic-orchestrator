[project]
name = "sdlc-orchestrator"
version = "1.0.0"
description = "SDLC orchestration using Claude Code CLI"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.0.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
